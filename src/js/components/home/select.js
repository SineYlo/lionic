/* eslint-disable no-unused-vars */
import Choices from 'choices.js';

const choices = new Choices('.form-join__field_country', {
  searchEnabled: false,
  itemSelectText: '',
  placeholder: true,
  allowHTML: false,
});
